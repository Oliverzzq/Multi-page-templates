import Vue from 'vue';
import '../css/base'


// import Vue from 'https://cdn.jsdelivr.net/npm/vue@2.6.11/dist/vue.esm.browser.js'

// app.el = '#app'
console.log(Vue);

new Vue({
    el: '#app',
    data: {
        title: '123'
    }
});